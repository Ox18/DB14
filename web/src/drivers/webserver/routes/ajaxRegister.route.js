const proxy = require("http-proxy-middleware");

module.exports = (router) => {
	// router.post("/ajaxRegister", (req, res) => {
	// 	// const { name, email, password, gender, year, month, day } = req.body;
	// 	// apiProxy.web(req, res, { target: "http://localhost:9002/api/register" });
	// });
	
};
